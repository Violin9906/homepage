language: node_js
node_js:
  - "12.13.1"

jobs:
  include:
    - if: branch = dev
      install:
        - "yarn install"

      script:
        - "yarn run build"
    - if: branch = master
      install:
        - "yarn install"
      script:
        - "yarn run build"
